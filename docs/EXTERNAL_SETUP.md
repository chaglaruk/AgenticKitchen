# External Setup

No provider is currently a production-ready integration. The app has legacy Gemini, Hugging Face, DuckDuckGo, and Pollinations paths; availability, credentials, structured parsing, validation, and error handling are still being migrated.

Do not enter a production credential until the Keystore-backed store and migration are integrated. The UI must not claim that credentials are securely stored until that runtime path exists.
